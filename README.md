# Optometry Clinic Electron — Release Channel

This repository hosts packaged installers and `latest.yml` metadata for the
Optometry Clinic Electron app. Updates are delivered automatically via
`electron-updater`. Do not edit releases manually unless rolling back.
